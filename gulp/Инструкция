для перехода между работой с pug и классическим html:
   В файле path, в объекте path.src и path.wath меняем в значении свойства html формат на .pug
   В файле таски html закомментировать .pipe(fileinclude())

npm run dev     - запуск сборки в режиме разработчика